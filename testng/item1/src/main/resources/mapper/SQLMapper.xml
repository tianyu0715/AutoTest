<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
        
<mapper namespace = "org.item1.model">
	<!-- 获取登录接口case -->
	<select id="loginCase" parameterType="Integer" resultType="org.item1.model.LoginCase">
		select * from logincase where id = #{id};
	</select>
	<!-- 添加用户接口case -->
	<select id="addUserCase" parameterType="Integer" resultType="org.item1.model.AddUserCase">
		select * from addusercase where id = #{id};
	</select>
	<!-- 获取用户信息case -->
	<select id="getUserInfoCase" parameterType="Integer" resultType="org.item1.model.GetUserInfoCase">
		select * from getuserinfocase where id = #{id};
	</select>
	<!-- 获取用户列表case -->
	<select id="getUserListCase" parameterType="Integer" resultType="org.item1.model.GetUserListCase">
		select * from getuserlistcase where id = #{id};
	</select>
	<!-- 更新/删除用户case -->
	<select id="updateUserInfoCase" parameterType="Integer" resultType="org.item1.model.UpdateUserInfoCase">
		select * from updateuserinfocase where id = #{id};
	</select>
</mapper>        